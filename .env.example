# Demo App Playwright Test Suite - Environment Configuration
#
# Default values are provided for the public demo application.
# Override these if testing against a different environment.

# Application Under Test (default: public demo app)
BASE_URL=https://animated-gingersnap-8cf7f2.netlify.app/

# Credentials (default: public demo app credentials)
# These defaults work out-of-the-box for the evaluation.
# Override for other environments.
USERNAME=admin
PASSWORD=password123

# Playwright Settings
HEADLESS=true
SLOW_MO=0
DEFAULT_TIMEOUT=30000
